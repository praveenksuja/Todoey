# Todoey

Todoey is a ios app that can allow us to create to do list items, we can organize, update, delete and even search specific items using search bar.
Nice gradient background will setup for every list item
It can persist all to do items even after closing the app.
Used  Swift4, XCode 9, UIKit, Core Data, Realm, User defaults, Core Graphics and Table views.
